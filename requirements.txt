# --- core (required) ---
bip-utils>=2.9.3          # BIP32/39/44 HD derivation (xpub -> per-order addresses)
eth-account>=0.11.0       # sign sweep transactions (dedicated hot-wallet mode)
eth-utils>=4.0.0

# --- API server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- QR for the hosted checkout (pure-python, no Pillow) ---
segno>=1.6.1

# --- admin dashboard (optional) ---
flask>=3.0.0

# --- loads a local .env file (so `cp .env.example .env` works without Docker) ---
python-dotenv>=1.0.0

# Standard library covers the rest (sqlite3, urllib, hmac, hashlib). No Redis, no
# MySQL, no node processes — one process, one SQLite file.
